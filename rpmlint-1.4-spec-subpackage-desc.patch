--- SpecCheck.py.orig	2013-04-22 13:19:41.011040011 +0400
+++ SpecCheck.py	2013-04-22 13:17:37.867028290 +0400
@@ -17,7 +17,7 @@
 import rpm
 
 from Filter import addDetails, printError, printWarning
-from TagsCheck import VALID_GROUPS
+from TagsCheck import VALID_GROUPS, max_line_len, use_utf8
 import AbstractCheck
 import Config
 import Pkg
@@ -232,6 +232,14 @@
 
                 continue
 
+            if current_section == 'description':
+                utf8line = line[:-1]
+                if use_utf8:
+                    utf8line = Pkg.to_utf8(utf8line).decode('utf-8')
+                utf8line.strip
+                if len(utf8line) > max_line_len:
+                    printError(pkg,'description-line-too-long', line)
+
             if current_section in ('prep', 'build') and \
                     contains_buildroot(line):
                 printWarning(pkg, 'rpm-buildroot-usage', '%' + current_section,
